const path = require('path');

module.exports = {
  'config': path.resolve('config', 'database.js'), // Ruta al archivo de configuración de la base de datos
  'models-path': path.resolve('models'), // Ruta donde se encuentran los modelos de Sequelize
  'seeders-path': path.resolve('seeders'), // Ruta donde se encuentran los archivos de seeders
  'migrations-path': path.resolve('migrations') // Ruta donde se encuentran los archivos de migraciones
};

